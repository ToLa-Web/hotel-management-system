FROM node:22-alpine3.20
WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci --omit=optional

# Source is mounted via volume — no COPY, no build step
EXPOSE 3000
CMD ["sh", "-c", "[ -d node_modules/next ] || npm ci --omit=optional && npm run dev"]
